#!/usr/bin/env bash
# base2-tp4 —— 单实例 4 卡 TP4 基线(★只用 4 张卡 0-3,另 4 张空闲★)。
# 作用:隔离出"算力减半"这一变量,用于把 PD 方案相对 base1 的差异,
# 拆成"TP8->TP4 算力损失" vs "PD 分离本身开销"两部分。
EXP_NAME=base2-tp4
MODE=single
CONNECTOR=none
BYPASS=none
TP=4
GPUS=0,1,2,3
PORT=8081
export EXP_NAME MODE CONNECTOR BYPASS TP GPUS PORT
